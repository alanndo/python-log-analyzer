# Python Log Analyzer

A lightweight Python CLI tool for analyzing Linux SSH authentication logs and detecting suspicious login behavior.

## Features

- Detects failed SSH login attempts
- Identifies suspicious IP addresses
- Tracks targeted usernames
- Detects successful logins after previous failures
- Exports results to JSON

## Project Structure
